name: 'Dev-Branch-ci'

on:
  push:
    branches: [ "ROBO-*" ]

permissions:
  contents: read

jobs:
  DevBranchCI:
    name: 'CI'
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Install nodejs dependencies
      run: git branch -a
  
#######
    